{
  "kind": "build_request",
  "title": "Update sex display format, rename app to SurgiPaw, and add dashboard with open tasks tally",
  "projectName": "SurgiPaw",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Change the sex field display format on the Case Card to show 'Female', 'Female Spayed', 'Male', or 'Male Neutered' instead of the current format",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-last"
        ],
        "quotes": [
          "On the Case Card Display Sex as: Female, Female Spayed, Male, or Male Neutered"
        ]
      },
      "acceptanceCriteria": [
        "The Case Card component displays sex as exactly 'Female', 'Female Spayed', 'Male', or 'Male Neutered'",
        "The display matches the underlying data values (male, neutered, female, spayed) defined in types.ts",
        "All existing cases show the updated format"
      ]
    },
    {
      "id": "REQ-2",
      "text": "Update the application name from 'Surgery Case Log' to 'SurgiPaw' in all user-facing locations including the page title, manifest, and any branding text",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-last"
        ],
        "quotes": [
          "Change app name to SurgiPaw"
        ]
      },
      "acceptanceCriteria": [
        "The browser tab title shows 'SurgiPaw'",
        "The PWA manifest displays 'SurgiPaw' as the app name",
        "Any header or branding text in the UI shows 'SurgiPaw'"
      ]
    },
    {
      "id": "REQ-3",
      "text": "Create a new Dashboard view that displays a list of all open tasks across all cases and provides a tally count of how many open tasks exist",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-last"
        ],
        "quotes": [
          "Create Dashboard that lists/provides a tally of open tasks"
        ]
      },
      "acceptanceCriteria": [
        "A new Dashboard view is accessible from the main navigation",
        "The dashboard shows a total count of open tasks across all surgery cases",
        "The dashboard lists all open tasks with relevant case information (medical record, pet name, task type)",
        "Tasks are considered 'open' if they are selected in the checklist but not yet completed",
        "The dashboard updates in real-time when tasks are completed or cases are modified"
      ]
    }
  ],
  "constraints": [
    "Maintain the existing sex value options (male, neutered, female, spayed) in the backend schema",
    "Do not modify Internet Identity authentication flow",
    "Preserve all existing case management functionality"
  ],
  "nonGoals": [
    "Changing the case form input for sex field",
    "Modifying the CSV import/export format for sex values",
    "Adding new task types beyond the existing seven"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}