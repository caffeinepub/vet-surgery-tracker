# Specification

## Summary
**Goal:** Add the uploaded logo to the header, landing page, and as the app icon/favicon.

**Planned changes:**
- Display the uploaded logo (image-1.png) in the header/navigation bar
- Display the uploaded logo on the landing page
- Set the uploaded logo as the app icon/favicon in multiple sizes
- Save the logo as a static asset in frontend/public/assets

**User-visible outcome:** The application displays the custom veterinary surgery logo in the header, on the landing page, and as the browser tab icon.
